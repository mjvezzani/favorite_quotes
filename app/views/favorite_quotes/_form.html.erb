<%= form_for(@favorite_quote) do |f| %>
  <% if @favorite_quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite_quote.errors.count, "error") %> prohibited this favorite_quote from being saved:</h2>

      <ul>
      <% @favorite_quote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quote %><br>
    <%= f.text_field :quote %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
